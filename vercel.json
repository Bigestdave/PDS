{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "cd pds-01-section && npm install && npm run build",
  "outputDirectory": "pds-01-section/dist",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
